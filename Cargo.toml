[package]
name = "localhostv3"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
mio = { version = "0.8", features = ["os-poll", "net"] }
uuid = { version = "0.8", features = ["v4"] }
chrono = "0.4"
mime_guess = "2.0"